# Adam Environment Variables
# 
# By default, Adam uses Codex OAuth (ChatGPT subscription).
# Run `docker compose run --rm adam openclaw setup` to authenticate.
#
# If you prefer API keys instead, uncomment and fill in:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
